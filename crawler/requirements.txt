# Bước 2 — cào Michelin (cần trình duyệt thật vì trang có AWS WAF)
playwright>=1.48

# Bước 3 — làm sạch văn bản tự do bằng Gemini
google-genai>=1.0

# Sau khi pip install, còn một bước nữa:
#     python -m playwright install chromium
